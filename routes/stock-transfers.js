import express                  from "express";
import InputsController         from '../controllers/InputsController.js';
import StockTransfersController from '../controllers/StockTransfersController.js';
import AuthController           from '../controllers/AuthController.js';

let router = express.Router();

/**
 * @swagger
 * /api/stock-transfers:
 *   post:
 *     tags:
 *       - Stock Transfers
 *     summary: Add a Stock Transfer
 *     requestBody:
 *       required: true
 *       content:
 *         application/json:
 *           schema:
 *             type: object
 *             required:
 *               - _sourceWarehouse
 *               - _destinationWarehouse
 *               - _product
 *               - count
 *             properties:
 *               _sourceWarehouse:
 *                 type: string
 *               _destinationWarehouse:
 *                 type: string
 *               _product:
 *                 type: string
 *               count:
 *                 type: number
 *     responses:
 *       400:
 *          description: Bad Request (for validation)
 *          content:
 *              application/json:
 *                  schema:
 *                      type: object
 *                      properties:
 *                          message:
 *                              type: string
 *                          errors:
 *                              type: array
 *                              items:
 *                                  type: string
 *       403:
 *          description: Forbidden
 *       401:
 *          description: Unauthorized
 *       200:
 *         description: Successful insert
 *         content:
 *           application/json:
 *             schema:
 *               type: object
 *               properties:
 *                 _id:
 *                   type: string
 *                 _user:
 *                   type: string
 *                 _sourceWarehouse:
 *                   type: string
 *                 count:
 *                   type: number
 *                 _product:
 *                   type: string
 *                 _destinationWarehouse:
 *                   type: string
 *                 _inventoryChanges:
 *                   type: string
 *                 status:
 *                   type: string
 *                 createdAt:
 *                   type: string
 *                 updatedAt:
 *                   type: string
 *                 createdAtJalali:
 *                   type: string
 *                 updatedAtJalali:
 *                   type: string
 */
router.post(
    '/',
    AuthController.authorizeJWT,
    AuthController.checkAccess,
    function (req, res, next) {

        // create clean input
        let $input = InputsController.clearInput(req.body);

        // add author to created unit
        $input.user = req.user;

        StockTransfersController.insertOne($input).then(
            (response) => {
                return res.status(response.code).json(response.data ?? {});
            },
            (error) => {
                return res.status(error.code ?? 500).json(error.data ?? {});
            }
        );
    }
);

/**
 * @swagger
 * /api/stock-transfers:
 *   get:
 *     summary: Get all Stock Transfers
 *     tags:
 *       - Stock Transfers
 *     parameters:
 *       - in: query
 *         name: createdAt
 *         schema:
 *           type: string
 *       - in: query
 *         name: _sourceWarehouse
 *         schema:
 *           type: string
 *       - in: query
 *         name: _destinationWarehouse
 *         schema:
 *           type: string
 *       - in: query
 *         name: _product
 *         schema:
 *           type: string
 *       - in: query
 *         name: status
 *         schema:
 *           type: string
 *       - in: query
 *         name: page
 *         schema:
 *           type: number
 *       - in: query
 *         name: perPage
 *         schema:
 *           type: number
 *       - in: query
 *         name: sortColumn
 *         schema:
 *           type: string
 *       - in: query
 *         name: sortDirection
 *         schema:
 *           type: number
 *     responses:
 *       400:
 *          description: Bad Request (for validation)
 *          content:
 *              application/json:
 *                  schema:
 *                      type: object
 *                      properties:
 *                          message:
 *                              type: string
 *                          errors:
 *                              type: array
 *                              items:
 *                                  type: string
 *       200:
 *         description: Successful get
 *         content:
 *           application/json:
 *             schema:
 *               type: object
 *               properties:
 *                 total:
 *                   type: number
 *                 list:
 *                   type: array
 *                   items:
 *                     type: object
 *                     properties:
 *                       _id:
 *                         type: string
 *                       count:
 *                         type: number
 *                       _sourceWarehouse:
 *                         type: object
 *                         properties:
 *                           _id:
 *                             type: string
 *                           title:
 *                             type: string
 *                       _destinationWarehouse:
 *                         type: object
 *                         properties:
 *                           _id:
 *                             type: string
 *                           title:
 *                             type: string
 *                       _product:
 *                         type: object
 *                         properties:
 *                           _id:
 *                             type: string
 *                           title:
 *                             type: string
 *                           code:
 *                             type: number
 *                           _unit:
 *                             type: object
 *                             properties:
 *                               _id:
 *                                 type: string
 *                               title:
 *                                 type: string
 *                       status:
 *                         type: string
 *                       createdAt:
 *                         type: string
 *                       updatedAt:
 *                         type: string
 *                       createdAtJalali:
 *                         type: string
 *                       updatedAtJalali:
 *                         type: string
 */
router.get(
    '/',
    function (req, res) {
        // create clean input
        let $input = InputsController.clearInput(req.query);

        StockTransfersController.transfers($input).then(
            (response) => {
                return res.status(response.code).json(response.data);
            },
            (error) => {
                const statusCode = error.code && typeof error.code === 'number' && error.code >= 100 && error.code < 600 ? error.code : 500;
                return res.status(statusCode).json(error.data ?? {});
            }
        );
    }
);

/**
 * @swagger
 * /api/stock-transfers/{id}:
 *   delete:
 *     summary: delete a Stock Transfer (return stocks)
 *     tags:
 *       - Stock Transfers
 *     parameters:
 *        - in: path
 *          name: id
 *          required: true
 *          schema:
 *            type: string
 *          description: The ID of the item to which the transfer belongs
 *     responses:
 *       400:
 *          description: Bad Request (for validation)
 *          content:
 *              application/json:
 *                  schema:
 *                      type: object
 *                      properties:
 *                          message:
 *                              type: string
 *                          errors:
 *                              type: array
 *                              items:
 *                                  type: string
 *       403:
 *          description: Forbidden
 *       401:
 *          description: Unauthorized
 *       200:
 *         description: Successful delete
 */
router.delete(
    '/:_id',
    AuthController.authorizeJWT,
    AuthController.checkAccess,
    function (req, res, next) {

        // get id from params and put into Input
        let $params = InputsController.clearInput(req.params);

        StockTransfersController.deleteOne($params).then(
            (response) => {
                return res.status(response.code).json(response.data);
            },
            (error) => {
                return res.status(error.code ?? 500).json(error.data ?? {});
            }
        );
    }
);

export default router;
